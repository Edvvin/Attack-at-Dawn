#include <stdio.h>
#include <stdlib.h>

int smth(char* str){
    printf("%s",str);
    return 0;
}
int main(int argc, char** argv) {
    smth("YOLO SWAGH!!");
    int i;
    scanf("%d",&i);
    return (EXIT_SUCCESS);
}

